<!DOCTYPE html>
<html lang="en">
  <head>
    <%= javascript_include_tag "application" %>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "CoffeeBook" %></title>
    <%= csrf_meta_tags %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
    <![endif]-->

    <%= stylesheet_link_tag "application", :media => "all" %>

    <link href="images/favicon.ico" rel="shortcut icon">
    <link href="images/apple-touch-icon.png" rel="apple-touch-icon">
    <link href="images/apple-touch-icon-72x72.png" rel="apple-touch-icon" sizes="72x72">
    <link href="images/apple-touch-icon-114x114.png" rel="apple-touch-icon" sizes="114x114">
  </head>
  <body>

    <div class="navbar navbar-fixed-top">
      <div class="navbar-inner">
        <div class="container">
          <a class="btn btn-navbar" data-target=".nav-collapse" data-toggle="collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </a>
          <%= link_to "Part time", root_path, :class => "brand"%>
          <%= render :partial => "shared/job_search_form"%>
          <%= render :partial => "shared/user_search_form"%>
          <div class="container nav-collapse">
            <ul class="nav pull-right">
              <%if !user_signed_in?%>
                <%= render 'shared/login_form' %>
              <% else %>
                <li><%= link_to current_user.name, user_accounts_path(current_user) %></li>
                <li><%= link_to "Sign out", destroy_user_session_path%></li>
              <%end%>
            </ul>
          </div><!--/.nav-collapse -->
        </div>
      </div>
    </div>
    <%= render 'layouts/flash_message' %>

    <div class="container">
      <div class="content">
        <div class="row">
           <div class="span9">
              <%= yield %>
            </div>
            <div class="span3">
              
              <div class="well sidebar-nav">
                <h3>Sidebar</h3>
                <ul class="nav nav-list">
                  <li class="nav-header">Sidebar</li>
                  <% if user_signed_in? %>
                    <li><%= link_to "My Account", user_accounts_path(current_user)  %></li>
                  <% end %>
                  <li><%= link_to "List Jobs", jobs_path  %></li>
                </ul>
              </div><!--/.well -->
              <% if user_signed_in? %>
                <% if current_page?(user_accounts_path(current_user))%>
                  <div class="well sidebar-nav">
                    <h3>Navigation Menu</h3>
                    <ul class="nav nav-list">
                      <li class="nav-header">Menu</li>
                      <li><%=link_to "Edit my details", edit_user_account_path(@user) %></li>
                      <li><%= link_to "Company", user_account_companies_path(@user) %></li>
                      <li><%= link_to "Review", user_account_reviews_path(@user) %></li>
                    </ul>
                  </div><!--/.well -->
                <% end %>
              <% end %>
              
            </div><!--/span-->
        </div><!--/row-->
      </div><!--/content-->

      <footer>
        <p>&copy; Brainbytes 2012</p>
      </footer>

    </div> <!-- /container -->



  </body>
</html>
